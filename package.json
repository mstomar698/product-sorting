{
  "name": "product-sorting",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install -f && cd ../frontend && npm install -f && npm run build",
    "start": "node backend/server.js"
  }
}
